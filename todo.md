# Da Big Mek Workshop - План Реализации (Версия для разработчика) 🟢⚙️

> *"Я не даю тебе готовый код. Я даю тебе направление. Ты должен сам понять КАК строить, чтобы понять ПОЧЕМУ это работает."*

---

## 📋 Философия этого плана

Этот план **НЕ содержит готового кода**. Вместо этого он:
- Описывает **проблему** которую нужно решить
- Объясняет **концепцию** которую нужно применить
- Даёт **ключевые слова** для самостоятельного изучения
- Показывает **структуру данных** но не реализацию
- Предлагает **вопросы** которые заставят думать

**Твоя задача:** самостоятельно написать код, основываясь на понимании концепций.

---

## Stage 0: "Da Rusty Start" - Почувствуй боль анемичной модели

> *Цель: Создать типичное Django приложение с сервисным слоем, чтобы ПОНЯТЬ какие проблемы оно создаёт*

### Контекст задачи

Орки собирают оружие из награбленных деталей. Мек (механик-орк) берёт несколько деталей из своей мастерской, собирает из них стреляло (shoota), и может из него выстрелить. Чем больше дакки (огневой мощи) у оружия - тем выше шанс попадания.

### Что нужно реализовать

#### 1. Модели данных (анемичные!)

**Workshop** - мастерская мека:
```
- id (UUID)
- mek_name (строка)
- reputation (целое число, по умолчанию 0)
- created_at (дата/время)
```

**Scrap** - лут для сборки:
```
- id (UUID)
- workshop (внешний ключ)
- name (строка, например "Ржавая труба")
- scrap_type (строка, например "metal", "shooty_bitz")
- quantity (целое число)
```

**Weapon** - оружие:
```
- id (UUID)
- workshop (внешний ключ)
- name (строка)
- dakka (целое число)
- parts_used (JSON - массив ID использованных деталей)
- created_at (дата/время)
```

**Важно:** Модели должны быть **анемичными** - только данные, никакой логики!

#### 2. Сервисный слой

Создай сервисы (классы или функции) для:

**WorkshopService:**
- Создание мастерской
- Добавление лута в инвентарь

**WeaponService:**
- Сборка оружия из доступных деталей
  - Нужно минимум 3 детали
  - Каждая деталь даёт +5 дакки
  - После сборки уменьшить quantity деталей
  - Увеличить reputation мастерской на +1
- Стрельба из оружия
  - Шанс попадания = dakka / 100
  - Вернуть "HIT" или "MISS"

#### 3. REST API

Создай endpoints (DRF или что удобно):
- `POST /api/workshops/` - создать мастерскую
- `POST /api/workshops/{id}/add_scrap/` - добавить лут
- `POST /api/workshops/{id}/build_weapon/` - собрать оружие
- `POST /api/weapons/{id}/fire/` - выстрелить

#### 4. Тесты

Напиши базовые тесты для сервисов через Django TestCase (с БД).

### Вопросы для размышления (ответь себе письменно)

После реализации задай себе эти вопросы:

1. **Где находится логика "сборки оружия"?** В одном месте или размазана?
2. **Что будет если нужно изменить правило расчёта дакки?** Сколько мест нужно изменить?
3. **Можно ли создать weapon с отрицательной дакой через ORM напрямую?** Что это значит?
4. **Как протестировать бизнес-правило "каждая деталь даёт +5 дакки" без БД?** Возможно ли?
5. **Что означает `parts_used = JSONField()`?** Понятно ли без чтения кода сервиса?

### Ключевые слова для изучения

- Anemic Domain Model (антипаттерн)
- Transaction Script pattern
- Service Layer pattern
- Django ORM basics

### Критерии готовности

- [ ] Можно создать мастерскую через API
- [ ] Можно добавить 3 детали через API
- [ ] Можно собрать оружие через API
- [ ] Можно выстрелить через API и получить HIT/MISS
- [ ] Написаны тесты с использованием БД
- [ ] Ты **письменно ответил** на вопросы для размышления

### Время

~15-20 часов

---

## Stage 1: "Da Propa' Way" - Rich Domain Model

> *Цель: Переместить бизнес-логику из сервисов в domain объекты. Понять разницу между Value Object и Entity. Научиться защищать инварианты.*

### Концепция которую изучаем

**Rich Domain Model** - модель где бизнес-логика живёт в самих объектах, а не в сервисах.

**Value Object** - объект без идентичности. Два VO с одинаковыми полями = один объект.  
**Entity** - объект с идентичностью. Два Entity с одинаковыми полями но разными ID = разные объекты.  
**Aggregate Root** - главный объект который контролирует доступ к вложенным объектам.

### Новые требования

Теперь у лута есть:
- **Качество** (quality): SHINY (лучшее), NORMAL, RUSTY (ржавое), EXPLODY (взрывоопасное)
- **Источник** (source): IMPERIAL (имперский), ELDAR (эльдарский), ORK (орочий), JUNK (хлам)

У оружия появляется:
- **Цвет** (color): RED (красный = +5 дакка!), BLUE, GREEN, UNPAINTED
- **Состояние** (condition): NEW, USED, DAMAGED, EXPLODED

Новые правила:
1. Можно покрасить оружие в красный → дакка увеличивается на +5
2. При выстреле есть шанс взрыва (зависит от качества деталей)
3. Имперские и эльдарские детали несовместимы (взорвутся)
4. Качество деталей влияет на дакку:
   - SHINY: +2 бонус
   - IMPERIAL источник: +3 бонус
   - RUSTY: -1 бонус

### Структура которую нужно создать

```
workshop/
├── domain/                    # НОВОЕ: domain слой
│   ├── primitives.py         # Enum'ы
│   ├── value_objects/        # Value Objects
│   ├── entities/             # Entities
│   ├── aggregates/           # Aggregates
│   ├── events/               # Domain Events
│   └── exceptions.py         # Domain исключения
├── application/              # НОВОЕ: application слой
│   ├── commands.py
│   ├── handlers/
│   └── repositories/
├── infrastructure/           # Существующие models.py
└── api/                      # Обновлённые views
```

### Задачи

#### 1. Создай Enum'ы для типов

В `domain/primitives.py` определи:
- `ScrapSource` (IMPERIAL, ELDAR, ORK, JUNK)
- `Quality` (SHINY, NORMAL, RUSTY, EXPLODY)
- `ScrapType` (ENGINE, ARMOR, SHOOTY_BITZ, METAL, WHEELS, ...)
- `Color` (RED, BLUE, GREEN, YELLOW, UNPAINTED)
- `Condition` (NEW, USED, DAMAGED, EXPLODED)

**Подсказка:** Используй Python `Enum`.

#### 2. Создай Value Objects

**Scrap** (лут):
```
Поля:
- source: ScrapSource
- scrap_type: ScrapType
- quality: Quality
- quantity: int

Методы которые нужны:
- is_compatible_with(other: Scrap) -> bool
  (имперское + эльдарское = несовместимо)
- with_quantity(new_quantity: int) -> Scrap
  (вернуть новый Scrap с другим количеством)

Требования:
- Иммутабельный (@dataclass(frozen=True))
- Валидация: quantity > 0
```

**DakkaRating** (огневая мощь):
```
Поля:
- base_dakka: int
- bonus_dakka: int

Свойства:
- total (вычисляемое): base + bonus

Методы:
- with_bonus(additional: int) -> DakkaRating
  (вернуть новую DakkaRating с дополнительным бонусом)
- __str__() для красивого вывода

Требования:
- Иммутабельный
```

**WeaponId, WorkshopId** (идентификаторы):
```
Поля:
- value: str (UUID)

Методы:
- generate() -> WeaponId (создать новый)
- from_string(s: str) -> WeaponId (из строки)

Требования:
- Иммутабельный
```

#### 3. Создай Entity - Weapon

**Weapon**:
```
Приватные поля:
- _id: WeaponId
- _name: str
- _parts: List[Scrap]
- _dakka: DakkaRating
- _color: Color
- _condition: Condition
- _malfunctions: int
- _events: List[DomainEvent]

Публичные свойства (только чтение):
- id, name, dakka, color, condition

Методы которые должны быть:
- paint_red() -> None
  Бизнес-правило: покрасить в красный
  - Проверить: уже не красное?
  - Изменить цвет на RED
  - Увеличить дакку на +5 (через with_bonus)
  - Опубликовать событие WeaponPaintedRedEvent
  
- fire(ork_count: int) -> FiringResult
  Бизнес-правило: выстрелить
  - Проверить: не взорвано?
  - Рассчитать WAAAGH бонус от количества орков
  - Проверить шанс взрыва (зависит от quality деталей)
  - Если не взорвалось: рассчитать попадание
  - Опубликовать событие
  
- _check_explosion() -> bool (приватный)
  Расчёт шанса взрыва:
  - Базовый шанс 5%
  - EXPLODY детали: +20% за каждую
  - RUSTY детали: +10% за каждую
  - Каждая поломка: +5%
```

**Вопрос для размышления:** Почему paint_red() это метод, а не просто `weapon.color = Color.RED`?

#### 4. Создай Aggregate Root - MekWorkshop

**MekWorkshop**:
```
Приватные поля:
- _id: WorkshopId
- _mek_name: str
- _inventory: List[Scrap]
- _weapons: Dict[WeaponId, Weapon]
- _reputation: int
- _events: List[DomainEvent]

Методы которые должны быть:
- loot_scrap(scrap: Scrap) -> None
  Простая операция: добавить в inventory
  
- build_shoota(parts_needed: List[ScrapType]) -> Weapon
  Сложная операция с инвариантами:
  1. Найти нужные детали в inventory
  2. ИНВАРИАНТ: достаточно ли деталей?
     Если нет → raise NotEnuffBitsError
  3. ИНВАРИАНТ: детали совместимы?
     Если нет → raise IncompatiblePartsError
  4. Рассчитать дакку с учётом качества
  5. Создать Weapon
  6. Удалить детали из inventory
  7. Добавить weapon в _weapons
  8. Увеличить reputation
  9. Опубликовать событие
  10. Вернуть weapon
  
- get_weapon(weapon_id: WeaponId) -> Optional[Weapon]
  Доступ к оружию только через агрегат!
  
- _calculate_dakka(parts: List[Scrap]) -> DakkaRating (приватный)
  Бизнес-правила расчёта:
  - Базовая дакка = 10
  - SHINY качество: +2
  - IMPERIAL источник: +3
  - RUSTY качество: -1
```

**Вопрос:** Почему нельзя изменять `_inventory` напрямую извне?

#### 5. Domain Events

Создай классы событий:
- `DomainEvent` (базовый с полем occurred_at)
- `ScrapLootedEvent`
- `WeaponBuiltEvent`
- `WeaponPaintedRedEvent`
- `WeaponFiredSuccessfullyEvent`
- `WeaponExplodedEvent`

**Подсказка:** Используй `@dataclass`.

#### 6. Domain Exceptions

Создай исключения:
- `NotEnuffBitsError`
- `IncompatiblePartsError`
- `AlreadyRedError`
- `CannotFireExplodedWeaponError`

#### 7. Repository Pattern

**Концепция:** Repository переводит между domain объектами и БД. Domain не знает о БД!

Создай:
- `WorkshopRepository` (абстрактный класс)
  - `get(workshop_id: WorkshopId) -> Optional[MekWorkshop]`
  - `save(workshop: MekWorkshop) -> None`
  
- `DjangoWorkshopRepository` (реализация)
  - Загрузка: ORM модели → domain объекты
  - Сохранение: domain объекты → ORM модели

**Ключевые слова:** Repository Pattern, Data Mapper Pattern, Persistence Ignorance

#### 8. Application Layer - Commands & Handlers

**Концепция:** Application слой координирует domain объекты, но не содержит бизнес-логики.

Создай команды:
- `CreateWorkshopCommand` (mek_name)
- `LootScrapCommand` (workshop_id, source, type, quality, quantity)
- `BuildShootaCommand` (workshop_id, parts_types)
- `PaintWeaponRedCommand` (workshop_id, weapon_id)
- `FireWeaponCommand` (workshop_id, weapon_id, ork_count)

Создай handlers для каждой команды:
```
Структура handler:
1. Загрузить aggregate через repository
2. Вызвать метод aggregate (domain логика)
3. Сохранить aggregate через repository
4. Опубликовать события (пока просто print)
5. Очистить события aggregate
```

#### 9. Обнови API

Перепиши views чтобы использовать Commands и Handlers вместо сервисов.

#### 10. Тесты domain слоя

**Важно:** Тесты domain объектов **БЕЗ БД**!

Напиши тесты для:
- Value Objects (иммутабельность, валидация)
- Weapon Entity (paint_red, fire, взрывы)
- MekWorkshop Aggregate (build_shoota, инварианты)

**Вопрос:** Чем эти тесты отличаются от тестов в Stage 0?

### Вопросы для размышления

1. **Где теперь находится логика расчёта дакки?** Сравни с Stage 0.
2. **Можно ли создать Weapon с отрицательной дакой?** Как это предотвращается?
3. **Как изменить правило "красное = +5 дакка"?** Сколько мест нужно изменить?
4. **Почему нельзя изменить weapon._dakka напрямую?** Что будет если попробовать?
5. **Зачем нужен Repository если есть Django ORM?**

### Ключевые слова для изучения

- Domain-Driven Design (основы)
- Value Object vs Entity
- Aggregate Root
- Invariant (инвариант)
- Bounded Context
- Repository Pattern
- Command Pattern
- Domain Events
- Rich Domain Model
- Ubiquitous Language

### Критерии готовности

- [ ] Domain слой создан и НЕ зависит от Django/БД
- [ ] Все бизнес-правила внутри domain объектов
- [ ] Инварианты защищены (нельзя создать невалидное состояние)
- [ ] Repository переводит между domain и ORM
- [ ] API работает через Commands/Handlers
- [ ] Тесты domain слоя БЕЗ БД работают
- [ ] Coverage domain слоя > 80%
- [ ] Ты ответил на вопросы для размышления

### Время

~40-50 часов

---

## Stage 2: "Da Kompleks Roolz" - Specifications & Domain Services

> *Цель: Научиться выражать сложные бизнес-правила через Specifications. Понять когда нужен Domain Service.*

### Концепция которую изучаем

**Specification Pattern** - способ инкапсулировать бизнес-правило в объект, который можно переиспользовать и комбинировать.

**Domain Service** - сервис для логики которая не принадлежит одному aggregate, но остаётся бизнес-логикой (не infrastructure).

### Новые требования

Появляются **навыки мека** (MekSkill):
- GROT (уровень грота, самый низкий)
- NORMAL (обычный мек)
- BIG_MEK (большой мек, опытный)

Появляются **blueprints** (чертежи оружия):
- Shoota (простое, complexity = 1)
- Big Shoota (сложнее, complexity = 2)
- Mega Blasta (очень сложное, complexity = 3)

Новые правила:
1. Мек может построить оружие только если его навык >= complexity чертежа
2. Разные blueprints требуют разные детали
3. Репутация мека влияет на качество оружия
4. **WAAAGH энергия** - чем больше орков поблизости, тем лучше работает оружие:
   - Базовая вера = количество орков
   - Нобзы (офицеры) дают больше веры
   - Боссы дают ещё больше
   - Репутация мека добавляет бонус

### Что нужно реализовать

#### 1. Specification Pattern

Создай базовый класс:
```
Specification[T]
  - is_satisfied_by(entity: T) -> bool
  - and_(other: Specification) -> AndSpecification
  - or_(other: Specification) -> OrSpecification
  - not_() -> NotSpecification
```

Создай спецификации:
- `CanBuildWeaponSpec` - может ли мек построить оружие
  - Проверяет: навык достаточен, детали есть, детали совместимы
- `PartsCompatibilitySpec` - совместимы ли детали
- `RedVehicleGoFasterSpec` - красное ли транспортное средство
  (на будущее для Stage 4)

**Вопрос:** Зачем нужна спецификация если можно просто `if` написать?

#### 2. Domain Service - WaaghEnergyService

Создай сервис который рассчитывает WAAAGH энергию:
```
WaaghEnergyService:
  - calculate_belief_power(
      workshop: MekWorkshop,
      weapon: Weapon,
      nearby_orks: List[Ork]
    ) -> int
```

Логика расчёта:
- Базовая вера = len(nearby_orks)
- Репутация мека: workshop.reputation // 10
- Ранг орков:
  - Грот (GROT) = 0 веры
  - Бой (BOY) = 1
  - Нобз (NOB) = 3
  - Босс (BOSS) = 5
  - Варбосс (WARBOSS) = 10
- Качество оружия:
  - NEW = +5
  - USED = +2
  - Остальное = 0

**Вопрос:** Почему это Domain Service а не метод в Workshop или Weapon?

#### 3. Модель Ork

Создай простую модель орка:
```
Ork (Value Object или Entity - реши сам):
  - name: str
  - rank: OrkRank (enum: GROT, BOY, NOB, BOSS, WARBOSS)
```

#### 4. Weapon Blueprints

Создай систему чертежей:
```
WeaponBlueprint (Value Object):
  - name: str
  - complexity: int
  - required_parts: List[ScrapType]
  - base_dakka: int
```

Предопределённые blueprints:
- SHOOTA_BLUEPRINT (complexity=1, 3 детали)
- BIG_SHOOTA_BLUEPRINT (complexity=2, 4 детали)
- MEGA_BLASTA_BLUEPRINT (complexity=3, 6 деталей)

#### 5. Обнови MekWorkshop

Добавь поле:
- `_mek_skill: MekSkill`

Измени метод `build_shoota` → `build_weapon`:
```
build_weapon(blueprint: WeaponBlueprint) -> Weapon
  - Проверить через CanBuildWeaponSpec
  - Если не проходит: why_not_satisfied() для понятного сообщения
  - Остальное как раньше
```

#### 6. Обнови Weapon.fire()

Добавь параметр `waaagh_energy: int`:
```
fire(waaagh_energy: int) -> FiringResult
  - Effective dakka = self._dakka.total + waaagh_energy
  - Остальное как раньше
```

#### 7. Application Handler для стрельбы

Обнови FireWeaponHandler:
```
1. Загрузить workshop
2. Получить weapon
3. Получить nearby_orks (откуда-то)
4. Рассчитать WAAAGH через WaaghEnergyService
5. Вызвать weapon.fire(waaagh_energy)
6. Сохранить
```

### Вопросы для размышления

1. **Чем Specification отличается от простого if?** Какие преимущества?
2. **Когда нужен Domain Service?** Приведи примеры когда нужен, когда нет.
3. **Почему WaaghEnergyService принимает и workshop, и weapon, и orks?**
4. **Можно ли было сделать WAAAGH расчёт методом Weapon?** Почему да/нет?
5. **Как комбинировать спецификации через and/or/not?**

### Ключевые слова для изучения

- Specification Pattern
- Composite Specification
- Domain Service vs Application Service
- Business Logic in Domain
- Tell Don't Ask principle

### Критерии готовности

- [ ] Specification Pattern реализован
- [ ] Можно комбинировать спецификации через and/or/not
- [ ] WaaghEnergyService работает
- [ ] Blueprints определены
- [ ] Мек не может построить слишком сложное оружие
- [ ] WAAAGH энергия влияет на стрельбу
- [ ] Тесты для спецификаций и domain services
- [ ] Ответы на вопросы

### Время

~25-30 часов

---

## Stage 3: "Da Separashun" - CQRS

> *Цель: Разделить модель записи и модель чтения. Понять eventual consistency.*

### Концепция которую изучаем

**CQRS (Command Query Responsibility Segregation)** - разделение операций изменения (Commands) и операций чтения (Queries) на разные модели.

**Event Sourcing** - сохранение не текущего состояния, а всех событий которые привели к нему.

**Read Model** - денормализованная модель оптимизированная под запросы.

### Проблема которую решаем

Текущая модель (PostgreSQL с нормализацией) плохо работает для:
- Сложных аналитических запросов (JOIN по 5 таблицам)
- Real-time дашбордов (постоянные SELECT)
- Исторических данных (audit trail)

### Новые требования

Нужны аналитические запросы:
1. **Статистика мастерской**:
   - Всего оружия создано
   - Всего взрывов
   - Success rate
   - Самое мощное оружие

2. **Топ оружия по дакке** (leaderboard)

3. **История всех событий** (audit trail)

4. **Аналитика по источникам лута**:
   - Какой лут чаще используется
   - Корреляция источника и взрывов

### Архитектура

```
Write Model (PostgreSQL)     Read Model (MongoDB)
      ↓                              ↑
   Commands                       Queries
      ↓                              ↑
   Domain ────Events────> Event Handlers ──> Projections
      ↓
   Event Store (append-only log)
```

### Что нужно реализовать

#### 1. Event Bus

Создай механизм публикации/подписки на события:
```
EventBus (интерфейс):
  - publish(event: DomainEvent) -> None
  - subscribe(event_type: Type, handler: EventHandler) -> None

InMemoryEventBus (реализация для начала)
```

**Ключевые слова:** Pub/Sub pattern, Message Bus, Event-driven architecture

#### 2. Event Store

Создай хранилище всех событий:
```
EventStore:
  - append(event: DomainEvent) -> None
  - get_events(aggregate_id, from_version) -> List[DomainEvent]
  - replay_all() -> Iterator[DomainEvent]
```

Структура записи в БД:
```
EventRecord:
  - event_id (UUID)
  - event_type (string)
  - aggregate_id (UUID)
  - payload (JSON)
  - occurred_at (timestamp)
  - version (int)
```

#### 3. Read Model - MongoDB схемы

Определи структуры для MongoDB:
```
workshop_stats:
  - workshop_id
  - total_weapons
  - total_explosions
  - success_rate
  - top_weapons (embedded array)
  - most_used_scrap
  - last_updated

dakka_leaderboard:
  - weapon_id
  - name
  - dakka
  - mek_name
  - created_at
  - is_exploded
```

#### 4. Event Handlers для проекций

Создай handlers которые обновляют Read Model:
```
WeaponBuiltEventHandler:
  - Увеличить workshop_stats.total_weapons
  - Добавить в dakka_leaderboard

WeaponExplodedEventHandler:
  - Увеличить workshop_stats.total_explosions
  - Пересчитать success_rate
  - Удалить из dakka_leaderboard
  - Пометить is_exploded = true

ScrapLootedEventHandler:
  - Обновить статистику по источникам лута
```

**Вопрос:** Что будет если handler упадёт с ошибкой? Как обработать?

#### 5. Query Handlers

Создай queries:
```
GetWorkshopStatsQuery:
  - workshop_id: WorkshopId

GetDakkaLeaderboardQuery:
  - limit: int

GetEventHistoryQuery:
  - aggregate_id: WorkshopId
  - from_date: datetime
```

Создай handlers которые читают из MongoDB:
```
GetWorkshopStatsHandler:
  - Читать из workshop_stats collection
  
GetDakkaLeaderboardHandler:
  - Читать из dakka_leaderboard, сортировать по dakka
```

#### 6. Обнови Command Handlers

Измени handlers чтобы публиковали события:
```
После save(aggregate):
  for event in aggregate.domain_events:
    event_store.append(event)      # Сохранить в Event Store
    event_bus.publish(event)        # Отправить в Read Model
  aggregate.clear_events()
```

#### 7. API для Queries

Создай новые endpoints:
- `GET /api/workshops/{id}/stats/` - статистика
- `GET /api/leaderboard/` - топ оружия
- `GET /api/workshops/{id}/history/` - история событий

### Challenges (Вызовы для размышления)

**Challenge 1: Eventual Consistency**

Read Model обновляется асинхронно. Что будет:
1. Создали оружие
2. Сразу запросили stats
3. Weapon ещё не появился в stats

Как обработать? Варианты:
- Показать "обновляется..."
- Синхронное обновление для критичных операций
- Версионирование

**Challenge 2: Event Replay**

Что если добавили новую проекцию? Нужно:
1. Очистить Read Model
2. Прочитать все события из Event Store
3. Прогнать через новые handlers

Реализуй метод `rebuild_projections()`.

**Challenge 3: Event Schema Evolution**

Изменили структуру события WeaponBuiltEvent. Старые события в Event Store имеют старую структуру. Как обработать?

### Вопросы для размышления

1. **Зачем разделять Write и Read модели?** Разве одной БД не достаточно?
2. **Что такое Eventual Consistency?** В чём отличие от Strong Consistency?
3. **Зачем Event Store если есть события в Event Bus?**
4. **Можно ли полностью восстановить состояние из событий?**
5. **Что делать если Read Model рассинхронизировалась?**

### Ключевые слова для изучения

- CQRS Pattern
- Event Sourcing
- Read Model / Write Model
- Eventual Consistency
- Projection
- Event Store
- Materialized View
- Command vs Query

### Критерии готовности

- [ ] Event Bus работает (pub/sub)
- [ ] Event Store сохраняет все события
- [ ] MongoDB настроена
- [ ] Read Model обновляется через Event Handlers
- [ ] Queries читают из Read Model
- [ ] Write и Read разделены в API
- [ ] Можно replay события для rebuild проекций
- [ ] Тесты для Event Handlers
- [ ] Ответы на вопросы

### Время

~35-40 часов

---

## Stage 4: "Da Long Waaagh" - Sagas

> *Цель: Научиться управлять долгими бизнес-процессами с несколькими шагами и возможностью отката.*

### Концепция которую изучаем

**Saga Pattern** - способ управления распределённой транзакцией через последовательность локальных транзакций с компенсирующими действиями.

### Проблема которую решаем

Построить сложную технику (грузовик Wartrukk) - это не одна операция:
1. Собрать шасси
2. Установить двигатель
3. Добавить броню
4. Покрасить в красный
5. Тест-драйв (может взорваться!)

Если тест-драйв провалился - нужно откатить изменения (вернуть детали).

### Новая бизнес-логика

**Vehicle** (транспорт):
```
Поля:
- id: VehicleId
- vehicle_type: VehicleType (WARTRUKK, BATTLEWAGON, DEFF_DREAD)
- chassis: ChassisType
- engine: Optional[EngineType]
- armor_plates: List[ArmorPlate]
- color: Color
- condition: Condition
- test_passed: bool
```

**Процесс сборки Wartrukk:**
```
1. BuildChassisCommand
   ↓ chassis_id
2. InstallEngineCommand
   ↓ engine_installed
3. AddArmorCommand
   ↓ armor_added
4. PaintVehicleCommand
   ↓ painted_red
5. TestDriveCommand
   ↓ result
   
Если result == EXPLODED:
  → CompensateExplosion (вернуть уцелевшие детали)
  
Если result == FAILURE:
  → CompensateAll (откатить всё)
```

### Что нужно реализовать

#### 1. Saga Infrastructure

Создай базовую инфраструктуру:
```
SagaStep:
  - execute() -> result
  - compensate() -> None

Saga:
  - steps: List[SagaStep]
  - execute() -> result
  - rollback(from_step: int) -> None

SagaState:
  - saga_id: SagaId
  - current_step: int
  - completed_steps: List[str]
  - failed: bool
  - compensation_done: bool
```

#### 2. BuildWartrukSaga

Реализуй сагу для постройки грузовика:
```
BuildWartrukSaga:
  Шаги:
  1. BuildChassisStep
     - execute: создать шасси
     - compensate: удалить шасси, вернуть детали
     
  2. InstallEngineStep
     - execute: установить двигатель
     - compensate: снять двигатель, вернуть
     
  3. AddArmorStep
     - execute: добавить броню
     - compensate: снять броню, вернуть
     
  4. PaintStep
     - execute: покрасить
     - compensate: (краску не вернёшь)
     
  5. TestDriveStep
     - execute: тест-драйв
     - compensate: оценить ущерб, вернуть уцелевшее
```

#### 3. Saga State Persistence

Сага должна сохранять своё состояние:
```
SagaRepository:
  - save(saga_id, state: SagaState) -> None
  - get(saga_id) -> Optional[SagaState]
  - update_step(saga_id, step: int) -> None
```

**Вопрос:** Зачем сохранять состояние саги? Что если процесс упадёт?

#### 4. Compensating Actions

Реализуй компенсации:
```
RecoverEngineCommand:
  - Найти двигатель в разбитой машине
  - Оценить состояние (DAMAGED обычно)
  - Вернуть в инвентарь мастерской

RecoverArmorPartsCommand:
  - Часть брони уцелела (50% обычно)
  - Вернуть в инвентарь

DisassembleChassisCommand:
  - Полностью разобрать шасси
  - Вернуть все детали
```

#### 5. Saga Orchestrator

Создай orchestrator который управляет сагами:
```
SagaOrchestrator:
  - start_saga(saga: Saga) -> saga_id
  - continue_saga(saga_id: SagaId, result) -> None
  - compensate_saga(saga_id: SagaId) -> None
```

#### 6. Async Execution (опционально)

Сделай шаги саги асинхронными:
```python
async def execute_saga(saga: BuildWartrukSaga):
    for step in saga.steps:
        result = await step.execute()
        if result.failed:
            await saga.rollback()
            break
```

**Ключевые слова:** asyncio, async/await, coroutines

### Challenges

**Challenge 1: Partial Failure**

Шаг 3 (AddArmor) выполнился, но процесс упал до сохранения. При перезапуске:
- В базе нет информации о шаге 3
- Но броня фактически установлена

Как обработать?

**Challenge 2: Idempotency**

Saga пытается retry шаг после сбоя. Шаг выполнился дважды. Как сделать шаги идемпотентными?

**Challenge 3: Long-Running Saga**

Saga выполняется 5 минут. Как показать прогресс пользователю? Как отменить сагу?

### Вопросы для размышления

1. **Чем Saga отличается от обычной транзакции?**
2. **Почему нельзя использовать database transaction для саги?**
3. **Что такое компенсирующая операция?**
4. **Чем Orchestration отличается от Choreography?**
5. **Как обработать ситуацию когда compensate() тоже падает?**

### Ключевые слова для изучения

- Saga Pattern
- Orchestration vs Choreography
- Compensating Transaction
- Distributed Transaction
- Two-Phase Commit (2PC)
- Eventually Consistent
- Idempotent Operation

### Критерии готовности

- [ ] Saga infrastructure создана
- [ ] BuildWartrukSaga работает
- [ ] Компенсации реализованы
- [ ] Saga State сохраняется
- [ ] Можно продолжить сагу после сбоя
- [ ] Тесты для саг
- [ ] Ответы на вопросы

### Время

~30-35 часов

---

## Stage 5: "Da Big Separashun" - Bounded Contexts

> *Цель: Разбить монолит на bounded contexts. Понять как интегрировать контексты.*

### Концепция которую изучаем

**Bounded Context** - явная граница внутри которой термины имеют определённый смысл. За границей те же термины могут значить другое.

**Context Map** - карта отношений между контекстами.

### Новая архитектура

Разбиваем систему на контексты:

```
┌────────────────────┐
│ MEK WORKSHOP       │ ← Наш основной контекст
│ CONTEXT            │
└────────────────────┘
         │
         ├──────────────────┐
         ↓                  ↓
┌────────────────┐  ┌──────────────┐
│ LOOTIN'        │  │ WAAAGH! HQ   │
│ CONTEXT        │  │ CONTEXT      │
└────────────────┘  └──────────────┘
         ↓                  ↓
┌────────────────────────────────┐
│ BATTLE CONTEXT                 │
└────────────────────────────────┘
         ↓
┌────────────────────────────────┐
│ BILLING CONTEXT                │
└────────────────────────────────┘
```

### Context Relationships

**Workshop → Lootin' (Customer/Supplier)**:
- Lootin' публикует `RaidCompletedEvent` с имперским лутом
- Workshop переводит через Anti-Corruption Layer в свои термины

**Workshop → WAAAGH! HQ (Published Language)**:
- Workshop публикует события на общем языке
- HQ подписывается и обновляет свою модель

**Workshop ↔ Battle (Shared Kernel)**:
- Общие Value Objects (DakkaRating, Color)
- Но разные модели Weapon!

**Workshop ⊥ Billing (Separate Ways)**:
- Полностью независимые
- Интеграция через HTTP API

### Что нужно реализовать

#### 1. Выдели Bounded Contexts

Реорганизуй проект:
```
da-big-mek-workshop/
├── shared-kernel/
│   └── domain/value_objects/
├── contexts/
│   ├── workshop/           # Главный контекст
│   ├── lootin/             # (упрощённая реализация для демо)
│   ├── waaagh_hq/          # (упрощённая реализация)
│   ├── battle/             # (упрощённая реализация)
│   └── billing/            # (отдельный сервис)
└── integration/
```

#### 2. Anti-Corruption Layer - Workshop ← Lootin'

Создай translator:
```
LootTranslationService:
  - translate_imperial_equipment(
      equipment: ImperialEquipment
    ) -> Scrap
    
    Перевод:
    - "Lasgun" → SHOOTY_BITZ, IMPERIAL, SHINY
    - "Rhino APC" → ENGINE, IMPERIAL, SHINY
    - "Power Armor" → ARMOR, IMPERIAL, NORMAL
```

Создай handler:
```
RaidCompletedEventHandler:
  1. Получить event от Lootin' контекста
  2. Перевести через LootTranslationService
  3. Добавить в Workshop через loot_scrap()
```

**Вопрос:** Зачем нужен translator? Почему нельзя использовать их модель напрямую?

#### 3. Published Language - Workshop → WAAAGH! HQ

Определи общий язык событий:
```
Common Events (understood by all contexts):
- WeaponBuiltEvent
- WeaponExplodedEvent
- WorkshopCreatedEvent
```

В HQ контексте:
```
Их модель Weapon (упрощённая):
  - id: str
  - type: str
  - power_level: int  # Не dakka!
  
HQEventHandler:
  - Слушает WeaponBuiltEvent
  - Преобразует в свою модель
  - Сохраняет в своей БД
```

#### 4. Shared Kernel - Workshop ↔ Battle

Вынеси общие VO:
```
shared-kernel/domain/value_objects/
  - dakka_rating.py
  - color.py
  - primitives.py
```

В Battle контексте создай свою модель:
```
BattleWeapon (Entity в Battle контексте):
  - id: WeaponId
  - firepower: DakkaRating  # Тот же VO!
  - paint: Color            # Тот же VO!
  - ammunition: int         # Своё поле
  - heat_level: int         # Своё поле
```

**Вопрос:** Weapon в Workshop и BattleWeapon в Battle - это один объект?

#### 5. Separate Ways - Workshop ⊥ Billing

Создай Billing как отдельный микросервис:
```
Billing Context (FastAPI или Flask):
  - MekAccount (Entity)
  - Payment (Value Object)
  - Invoice (Entity)
  
  API:
  - POST /billing/charge
  - GET /billing/account/{mek_name}
```

В Workshop создай клиент:
```
BillingClient:
  - charge_for_build(
      mek_name: str,
      complexity: int
    ) -> bool
```

Интеграция:
```
BuildWeaponHandler:
  1. Проверить payment через BillingClient
  2. Если оплата прошла → продолжить
  3. Если нет → raise InsufficientFundsError
```

#### 6. Docker Compose для всех контекстов

Настрой infrastructure:
```yaml
services:
  workshop-api:
    # Django app
  
  lootin-api:
    # Упрощённый сервис для демо
  
  waaagh-hq-api:
    # Упрощённый сервис для демо
  
  billing-api:
    # FastAPI микросервис
  
  workshop-db:
    # PostgreSQL для Workshop
  
  workshop-read-db:
    # MongoDB для Workshop Read Model
  
  billing-db:
    # PostgreSQL для Billing
  
  event-bus:
    # RabbitMQ или Kafka
```

### Challenges

**Challenge 1: Distributed Transaction**

User создаёт оружие:
1. Workshop списывает детали
2. Billing списывает деньги

Если Billing падает после списания деталей - как откатить?

**Challenge 2: Eventual Consistency Between Contexts**

Workshop создал оружие, HQ ещё не обновился. User смотрит в HQ - оружия нет. Как обработать?

**Challenge 3: Context Versioning**

Workshop изменил структуру WeaponBuiltEvent. HQ всё ещё ожидает старую структуру. Как мигрировать?

### Вопросы для размышления

1. **Что такое Bounded Context?** Приведи примеры границ.
2. **Чем отличается Weapon в Workshop от Weapon в Battle?**
3. **Зачем нужен Anti-Corruption Layer?**
4. **Что такое Shared Kernel?** В чём риски?
5. **Когда использовать Published Language vs ACL?**
6. **Как интегрировать контексты?** Какие способы есть?

### Ключевые слова для изучения

- Bounded Context
- Context Map
- Anti-Corruption Layer (ACL)
- Published Language
- Shared Kernel
- Customer/Supplier
- Conformist
- Separate Ways
- Open Host Service
- Microservices
- Service Mesh

### Критерии готовности

- [ ] Проект разбит на контексты
- [ ] ACL реализован (Workshop ← Lootin')
- [ ] Published Language работает
- [ ] Shared Kernel вынесен
- [ ] Billing как отдельный сервис
- [ ] Docker Compose поднимает всю систему
- [ ] Контексты общаются через события/HTTP
- [ ] Context Map нарисован
- [ ] Ответы на вопросы

### Время

~40-45 часов

---

## Финальная документация

После завершения всех этапов создай:

### 1. Эволюционная диаграмма

Нарисуй как архитектура эволюционировала:
```
Stage 0 → Stage 1 → Stage 2 → Stage 3 → Stage 4 → Stage 5
```

Для каждого перехода:
- Какая была проблема
- Какой паттерн применили
- Что улучшилось
- Что усложнилось

### 2. Сравнительная таблица

| Aspect | Stage 0 | Stage 5 |
|--------|---------|---------|
| Lines of code | ??? | ??? |
| Test coverage | ??? | ??? |
| Time to add feature | ??? | ??? |
| Понятность | ??? | ??? |

### 3. Lessons Learned

Напиши что ты понял:
- Когда нужен каждый паттерн
- Какие ошибки сделал
- Что бы сделал иначе
- Какие принципы DDD самые важные

### 4. README для каждого stage

В каждой ветке создай README:
- Что реализовано
- Как запустить
- Какие проблемы решает
- Что дальше

---

## Общие рекомендации

### Как работать с этим планом

1. **Не читай код других** - пиши сам
2. **Гугли концепции** - не реализацию
3. **Экспериментируй** - пробуй разные подходы
4. **Рефакторь** - первая версия всегда плохая
5. **Отвечай на вопросы** - это важнее кода

### Когда застрял

1. Перечитай концепцию
2. Погугли ключевые слова
3. Нарисуй диаграмму на бумаге
4. Попробуй объяснить проблему резиновой уточке
5. Отдохни и вернись со свежей головой

### Чего НЕ делать

- ❌ Копировать готовый код
- ❌ Пропускать вопросы для размышления
- ❌ Делать всё быстро ради скорости
- ❌ Игнорировать тесты
- ❌ Двигаться дальше не поняв предыдущий этап

### Чего делать

- ✅ Писать код самостоятельно
- ✅ Отвечать на вопросы письменно
- ✅ Коммитить часто
- ✅ Писать тесты
- ✅ Документировать решения

---

## Заключение

Этот проект - **твоё путешествие** к пониманию архитектуры.

Не спеши. Понимание важнее скорости.

**WAAAGH! NOW GO BUILD IT YERSELF!** 🟢⚙️💥
